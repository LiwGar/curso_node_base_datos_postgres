version: '3.3'

services:
  postgres:
    image: postgres:13
      environment:
        - POSTGRES_DB=mystore
        - POSTGRES_USER=nico
        - POSTGRES_PASSWORD=admin123
      ports:
        - 5432:5432
